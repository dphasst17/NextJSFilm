export const createItemsMessage = (items:String) => {
    return `Create ${items} is success`
}
export const updateItemsMessage = (items:String) => {
    return `Update ${items} is success`
}
export const deleteItemsMessage = (items:String) => {
    return `Delete ${items} is success`
}